name: Build And Push Docker Image

on:
  push:
    branches:
      - master
    paths:
      - api/**
      - cmd/**.go
      - deployment/**
      - go.mod
      - go.sum
    tags:
      - "v*.*.*"

jobs:
  build:
    # NOT support strategy on reusable workflow
    # strategy:
    #   fail-fast: false
    #   matrix:
    #     service: [api]
    uses: mintak21/actions/.github/workflows/build.yml@f42f83968ed943556826e350fd6a6a78881dcaaf
    with:
      image: ${{ github.repository }}/api
      dockerfile: deployment/dockerfile/api/Dockerfile
